# Dependencies
node_modules/

# Expo
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts

# Native projects generated by `expo prebuild` / `expo run:*`.
# These are regenerated from app.json + config plugins, so they're not committed.
# (Delete these two lines if your team decides to commit the native projects.)
/android
/ios

# Native build artifacts
*.keystore
!debug.keystore
*.jks

# Metro / Babel
.metro-health-check*
.kotlin/

# Logs
npm-debug.*
yarn-error.*
*.log

# macOS / editors
.DS_Store
*.pem
.idea/
.vscode/

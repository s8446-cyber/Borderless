{
  "name": "borderless-pay-mobile",
  "version": "1.4.2",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "java:check": "node scripts/java-compat.js --check",
    "prebuild": "expo prebuild && node scripts/java-compat.js",
    "prebuild:clean": "expo prebuild --clean && node scripts/java-compat.js",
    "run:android": "npm run prebuild && expo run:android",
    "run:android:release": "npm run prebuild && expo run:android --variant release",
    "phone": "npm run prebuild && expo run:android --variant release",
    "run:ios": "expo run:ios"
  },
  "dependencies": {
    "expo": "~51.0.28",
    "expo-camera": "~15.0.16",
    "expo-local-authentication": "~14.0.1",
    "expo-secure-store": "~13.0.2",
    "expo-status-bar": "~1.12.1",
    "react": "18.2.0",
    "react-native": "0.74.5"
  },
  "private": true
}
